services:
  # ========================================
  # BACKEND PHP (já funcionando)
  # ========================================
  - type: web
    name: growth-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: growth-db
          property: connectionString

  # ========================================
  # FRONTEND ANGULAR (NOVA CONFIGURAÇÃO)
  # ========================================
  - type: web
    name: growthinvestimentos
    runtime: static
    buildCommand: cd Growth && npm install && npm run build
    staticPublishPath: ./Growth/www/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html